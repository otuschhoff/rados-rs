[graph]
all-features = false
no-default-features = false

[advisories]
version = 2
yanked = "deny"
ignore = []

[licenses]
version = 2
confidence-threshold = 0.8
unused-allowed-license = "allow"
allow = ["Apache-2.0", "LGPL-2.1-only", "MIT", "Unicode-3.0", "Unlicense"]
exceptions = []

[bans]
multiple-versions = "deny"
wildcards = "deny"
highlight = "all"
allow = []
deny = []
skip = [
	{ crate = "syn@2.0.119", reason = "Tokio macros remain on syn 2 while the exact Serde derive pin uses syn 3; both are build-time proc macros." },
	{ crate = "windows-sys@0.59.0", reason = "Tokio 1.47 retains this target-only shim while mio/socket2 use 0.61; R04 is qualified on macOS and linux/arm64." },
]
skip-tree = []

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []